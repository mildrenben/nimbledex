// Adding this code to the all.js via Gulp or simply moving it to the ../js/
// dir and it will incrementally run through every page. Handy to see if it
// stops on a page you know it has errors

// setTimeout(() => {
//   window.location.href = parseInt(window.location.href.slice(window.location.href.indexOf('8080') + 5, window.location.href.length)) + 1;
// }, 500);
